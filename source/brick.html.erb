---
title: Brick
---

<h1 class="brick-title"><%= label %></h1>

<img src="<%= path_to_svg(id, 'bricks') %>"/>

<% definitions = data.definitions.select { |defn| defn.bricks.include?(id) } %>

<% unless definitions.empty? %>
  <h2>Definitions that make use of this brick</h2>

  <%= partial('definitions-table', locals: {definitions: definitions.sort_by(&:word)}) %>
<% end %>
